import 'package:flutter/material.dart';

class SpaceHome extends StatelessWidget {
  const SpaceHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Space Home")));
  }
}
